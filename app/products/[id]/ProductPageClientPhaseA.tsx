// Cache-busted Product PDP client for Phase A pilots (morph + PDRN helper).
// Bump: helper always-visible slot + direct import (no V2 re-export).
export { default } from './ProductPageClientRefactored'
