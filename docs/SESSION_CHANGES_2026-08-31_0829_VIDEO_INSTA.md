# Desktop 0829.mov → Instagram 9:16

Date: 2026-08-31

Source: `~/Desktop/0829.mov` — 30s, 1920×1080 landscape, HEVC.
Same layout as the 28 Aug BBC clips: a 608×1080 vertical picture
with black bars. Small Ai mark sat in the left bar.

Original left in place.

## Output

| File | Size | Length |
|---|---|---|
| `~/Desktop/0829_insta.mp4` | 1080×1920 | 30s |

Crop `608:1080` from x=656. Scale to 1080×1920. H.264 CRF 18, 30 fps,
no audio (source audio was 2 kb/s empty). Ai mark is gone with the bars.

Pale-blue powder pour. Fits Hydro Cool Modeling Mask. Ready for Reels /
Stories. Not published.

## Overlay removed (same day)

Closing pouch shot had a burned-in AI card from **26.3s to the end**:

```
FORMULA COMPOSITION.
The fresioral teal mix corgelitten
HYGLLI 1% · … HO DELING MASK
```

First pass only killed dark glyphs and left a ghost. Second pass
(same day): from **25.0s to the end**, the whole studio band above
the zipper is filled from the top of the frame. Pack label untouched.
First pouch shot (~6–10s) never had the card.

Overwritten: `~/Desktop/0829_insta.mp4` and
`~/Desktop/Insta_Olga/hydro_o/hydro_insta/0829_insta.mp4`.
`0829.mov` untouched. Closing export rebuilt from this file.

## Closing end card (same day)

Copy of the clean reel, original `0829_insta.mp4` left as-is.

`Closing.jpeg` (1080×1350) padded to 1080×1920 with studio white
`#FEFEFE` from the top-left 40 px. Bars 285 px top and bottom. Not cropped.
Held **3 seconds** after the 30s reel.

| File | Length |
|---|---|
| `~/Desktop/Insta_Olga/hydro_o/hydro_insta/0829_insta_closing.mp4` | 33s |
| `~/Desktop/0829_insta_closing.mp4` | same copy |

H.264 CRF 18, 30 fps, no audio. Last frame is the Closing slide
(HYDRO COOL MODELING MASK / 1 KG / SHOP GENOSYS.AE / GENOSYS UAE APP).
