/**
 * @deprecated Import directly from '@/lib/' instead.
 * This directory is maintained for backward compatibility.
 * New utility functions should be added to lib/ directly.
 */

// Re-export for backward compatibility
// Future code should import from @/lib/ instead
